<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong><%= f.label "Titulo" %></strong><br><br>
    <%= f.text_field :title %>
  </div>
  <br>
  <div class="field">
    <strong><%= f.label "Descrição" %></strong><br><br>
    <%= f.text_area :description %>
  </div>
  <br>
  <div class="field">
    <strong><%= f.label "Data de postagem" %></strong><br><br>
    <%= f.datetime_select :when_posted %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Submeter!" %>
  </div>
<% end %>
